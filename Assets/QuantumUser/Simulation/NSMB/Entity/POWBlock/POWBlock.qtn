// in PowBlock.qtn
component PowBlock {
  byte RemainingCharges;
  byte SpriteState;
  FP ExplosionRadius;
}

signal OnPowHitEntity(EntityRef pow, EntityRef hitEntity);

event PowBlockExploded {
  EntityRef Entity;
}